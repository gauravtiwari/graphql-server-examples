# Elixir based web frameworks

A set of GraphQL server implementation on popular Elixir web frameworks.

## Frameworks
* Phoenix

```bash
$ brew update
$ brew install elixir
```

Then, follow the example readme to setup application locally.
